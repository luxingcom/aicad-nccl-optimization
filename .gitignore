# 排除编译产物 / 二进制 / 凭据
*.so
*.so.*
*.tar.gz
*.tgz
*.bundle
*.log
*.pyc
__pycache__/
.DS_Store

# 凭据与密钥（防御性：即使误拷也永不提交）
secrets/
*.env
*.pem
*.key
id_rsa*
id_ed25519*

# 原始运行数据（大数据文件，仅保留结论文档；config/daemon.json 为受控配置，保留）
verification-logs/
*.csv
