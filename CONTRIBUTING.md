# 贡献指南 / Contributing

感谢关注本项目。本项目为学习交流用途的 NCCL 通信优化资料包，接受以下形式的贡献。

## 可以提交的内容

- 文档勘误（步骤错误、失效命令、过时参数）
- 补丁在更多环境下的实测结果（附完整命令与输出摘要）
- 工具链改进（`tools/` 下的脚本的健壮性修复）
- 新的 ADR（架构决策记录），说明问题 → 备选 → 决策 → 验证

## 提交规范

- Conventional Commits：`fix:` / `feat:` / `docs:` / `chore:`
- 一个提交只做一件事
- 涉及补丁的修改必须说明针对的 NCCL 基线版本（当前 v2.30.7-1）

## 纪律

- **禁止**提交任何凭据、密码、API key、内网拓扑明细（IP 段请用 `192.0.2.x` 文档地址替代）
- **禁止**提交二进制产物（.so / .a / 编译缓存）
- 涉及行为变更的补丁必须附 A/B 数据与回滚方式
- 所有 shell 脚本改动须通过 `bash -n` 语法检查

## 许可

提交即表示同意以 Apache-2.0 许可（见 LICENSE）授权你的贡献。
