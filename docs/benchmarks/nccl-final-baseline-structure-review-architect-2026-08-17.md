# 最终性能基线文档结构审核（Archi / 架构）

**日期:** 2026-08-17
**作者:** Archi（系统架构师）
**审核对象:** `nccl-final-performance-baseline-2026-08-17.md`（Tessa / QA，v0.9 草稿）
**上游:** `nccl-benchmark-v2-final-qa-2026-08-16.md`（v2 v1.0 定版）/ `nccl-benchmark-v2-finalization-prep-qa-2026-08-16.md`（判读协议）
**性质:** 只读结构审核——核验最终基线文档结构完整性（非数值判读，数值判读权在 QA）；不执行测试、不碰生产
**状态:** 结构审核完成——**主体结构完整，建议补充 3 项后随 FINALBASE 定版 v1.0**

---

## 0. 审核结论（TL;DR）

对照任务要求完整性清单（32 档全表 / v1 锚定 / dspark / 并发放大 / 生产终态 / 与 v0.1/v1.0 对比），文档 **6/6 全部覆盖**：

| 完整性项 | 文档章节 | 判定 |
|---|---|---|
| 32 档全表 | §4（DE 12 + PR 20，含 FIX 补齐与 purity 标注） | ✅ 完整 |
| v1 锚定 | §5（C1 32K/131K PR + C4 32K agg + DE decode agg） | ✅ 完整 |
| dspark | §6（DE 12 档接受率 + per_pos 塌缩判读） | ✅ 完整 |
| 并发放大 | §7（wall-agg / per-request / per-slot 三层演进） | ✅ 完整 |
| 生产终态 | §1（环境快照 + NCCL env 实测）+ §10（终态确认 + 回滚锚点） | ✅ 完整 |
| 与 v0.1/v1.0 对比 | §3（v1 三级历史）+ §8（FINALBASE vs v2 v1.0 逐档表） | ✅ 完整（§8 待回填） |

**结构建议（3 项，非阻塞）**：① §8 需补"FINALBASE 数据来源路径 + 与 v2 目录对应关系"；② 回滚触发条件（§10.2）建议显式引用 v2 档位口径已重标（当前正文有一句，建议展开为阈值表）；③ 文档状态随 FINALBASE 到位从 v0.9 升 v1.0 时，需在 §0 增补"定版确认行"。

---

## 1. 逐项完整性审核

### 1.1 32 档全表（✅）

- §4.1 DE 12 档：TTFT / prefill / decode / wall-agg / 接受率 全列，接受率含样本计数（3/3、6/6…）——优于 v2 定版（仅 1.0），可追溯性更好。
- §4.2 PR 20 档：来源标注（v0.1 / FIX 补齐 / FIX 复跑）清晰，与 v2 v1.0 表完全一致。
- §4.3 purity：DE 全 PURE、PR 长档 PURE、唯一 IMPOLLUTED（PR_C6_L131076）已披露；**新增已知现象说明（C6 max running=5 系统现象）对 FINALBASE 判读非常关键**——避免补测同现此形被误判为新增污染。✅

### 1.2 v1 锚定（✅）

- §5 四组锚定：32K PR@C1（-0.4%/-1.8%）、131K PR@C1（-2.4%/-2.6%）、C4 32K agg（-0.8%）、DE decode agg@C4（+6.6%，口径注清晰）。均 🟢。
- **口径注（ctx 差 0-4 tokens 可直比 / DE max_tokens 512 vs 4096 仅趋势参考）**——比 v2 定版更严谨，正确处置了 DE 口径不可直比问题。✅

### 1.3 dspark（✅）

- §6 接受率区间（coding 0.77–0.88 / json 0.80–0.88 / prose 0.29–0.43）+ 随并发略升趋势 + **per_pos 尾位塌缩判读（引擎按批大小降档，非缺陷）**——判读有理有据。✅

### 1.4 并发放大（✅）

- §7 三层结构：wall-agg（C1→C6 放大）、per-request 衰减（C6/C1）、per-slot 效率——覆盖"批量吞吐↑/单请求延迟↑/槽效率递减"三个维度，与 v1 followup 结论（c4 agg decode +96.4%、prefill 饱和）一致并延展到 C6。✅

### 1.5 生产终态（✅）

- §1.1-1.4 环境快照完整：硬件/拓扑/软件栈/NCCL env（docker inspect 实测）/引擎参数。
- §10.1 终态确认（2be94172 + running=0）+ §10.2 回滚锚点（库/脚本/源码存档 + 触发条件）。✅

### 1.6 与 v0.1/v1.0 对比（✅，§8 待回填）

- §3 v1 三级历史表（A0/T1aM4/+MAX_CH16/+StageB）+ 累计收益表（368KB -81%、32K +15%、131K +22%、TTFT -17%）——历史纵向完整。
- §8 FINALBASE vs v2 v1.0 逐档表（DE 12 + PR 20，Δ±5% 判定）——**结构与 v2 定版一致，可直接回填判读**；§8.1 DQ 门禁 + §8.3 判读要点齐备。⚠️ 待 Rex 回传数据后回填。

---

## 2. 结构建议（3 项，非阻塞）

### 建议 1（中）：§8 补 FINALBASE 数据源说明
当前 §8 仅"待 Rex 回传路径"；建议在 §8 表前补一行：
> 数据源：`/opt/aicad-prod/verification-logs/FINALBASE_*/`（Rex 回传；含 summary/rows/manifest/monitor/precheck/bench_cfg 六件套，与 v2 目录结构一一对应）

保证判读时可对齐目录结构，避免本地副本复算时路径歧义（v2 曾出现本地 benchv2-data-fix 未检索到的问题）。

### 建议 2（中）：§10.2 回滚阈值展开为表
当前正文一句"32K PR < 2425 且 DE < 98.6 且 TTFT > 11.9s…"易读但建议展开为与 §5 锚定档一致的三行表（32K / 131K / FINALBASE 系统性超差），作为最终基线文档的正式回滚门禁（文档最终读者为运维/恢复人员，阈值表比散句更可操作）。

### 建议 3（低）：v1.0 定版时增补定版确认
升版时在 §0 增补一行"**定版确认**：FINALBASE 32/32 过 DQ 且逐档 Δ≤±5% → 确认 v2 v1.0 为最终基线；超差档标注查因后按 §8.3 处置"——与 QA 判读协议 §5 决策树保持一致。

---

## 3. 与归档清单的衔接提示

- 本基线文档（最终版 v1.0）建议纳入 2-hop 归档清单的 reports/ 之外、或单独归档为 `production-baseline/`——它是 RING 基线"定版收尾"的直接证据，与 `nccl-2hop-archive-manifest-v1` §2 类别 6（全部报告）同源，但属于**生产侧最终基线**而非 2-hop 线，归档时建议单列或交叉引用，避免混入 2-hop 实验报告。
- FINALBASE 补测数据落盘后，`/opt/aicad-prod/verification-logs/FINALBASE_*/` 应在归档清单中登记（可并入 §3 门数据表或单独小节）。

---

## 4. 审核结论

**主体结构完整，符合最终基线文档要求（32 档全表 + v1 锚定 + dspark + 并发放大 + 生产终态 + 与 v0.1/v1.0 对比 6/6 覆盖）。** 三处结构建议（§8 数据源说明 / §10.2 回滚阈值表 / v1.0 定版确认行）为增强项，不阻塞 FINALBASE 判读。FINALBASE 数据到位后按 §8 回填即可升 v1.0。

---

## 5. 数据来源
- 审核对象：`nccl-final-performance-baseline-2026-08-17.md`（本地 + 服务器，v0.9 草稿）
- 上游：`nccl-benchmark-v2-final-qa-2026-08-16.md` / `nccl-benchmark-v2-finalization-prep-qa-2026-08-16.md`
- 服务器实查：`/opt/aicad-prod/verification-logs/`（FINALBASE 未到位，Rex 执行中）

---

*文档生成：2026-08-17（最终性能基线文档结构审核——6/6 覆盖确认，3 项增强建议）*
