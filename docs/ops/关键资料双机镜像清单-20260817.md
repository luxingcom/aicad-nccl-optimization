# 关键资料双机镜像清单 (01 ↔ 02)

- 日期: 2026-08-17
- 作者: SRE 工程师 Rex
- 目的: 关键插件/补丁/源码/文档在 <node1> 与 <node2> 双机互为备份

## 1. 镜像范围与结果

| 目录 | 01 源 | 02 目标 | 同步状态 | 校验 |
|------|-------|--------|---------|------|
| /opt/aicad-prod/backup/ | 39G | /opt/aicad-prod/backup/ | 完成 | rsync -i dry-run changes=0; MD5-RECORD 一致 |
| /opt/aicad-prod/scripts/ | v027-test 已删除 (01 释放 8.2G) | /opt/aicad-prod/scripts/ | 完成 | changes=0 |
| /opt/aicad-prod/docs/ | 340K | /opt/aicad-prod/docs/ | 完成 | changes=0 |
| /opt/aicad-prod/deliverables/ | 892K | /opt/aicad-prod/deliverables/ | 完成 (02 新建) | changes=0 |
| /opt/2hop-s1/ | 641M → **原目录已删除(2026-08-17)** | backup/2hop-s1-archive-20260817.tar.gz (372M 刷新版) | tar 校验 diff=0 后删除; 已随 backup 双份 | md5 双机一致 |
| /opt/nccl-ringonly/ | 289M | 两机均有 (02 已存在) | 未覆盖传输 | libnccl.so.2.30.7 md5=2be94172... 双机一致 |

## 2. MD5 校验结果 (双机抽查, 全部一致)
| 文件 | MD5 |
|------|-----|
| backup/2hop-s1-archive-20260817.tar.gz (刷新版) | 4070d28c23aca62a992759efba607528 |
| scripts/start_tp4_head.sh | 3a3e02ba4daf941d41099cb20a2f38e1 |
| docs/runbook-tp4-v1.5-2026-08-12.md | 1a2072f5dc8210d41ed46d4d903ed8aa |
| nccl-ringonly/libnccl.so.2.30.7 (生产库) | 2be94172c1172734d00dee9ff7d788bd |
| backup/nccl-2hop-proto-archive-20260817/MD5-RECORD.txt | 26a8a2b6245f7d74202d7fdfa4997c0b |
| backup/nccl-official-2307-hardened-20260816/MD5-RECORD.txt | 287ec8908a09db2a4ce081608fdf7309 |

一致性证明: 01→02 各目录 `rsync -a --dry-run -i` 变更数均为 0。
backup 文件计数: 01=129336, 02=129399 (02 多 63 个为 02 本地原有 rollback/grafana 等文件, 因不用 --delete 予以保留)。

## 3. 同步脚本
`/opt/aicad-prod/scripts/mirror_to_02.sh` (已安装 + bash -n 通过)

设计决策:
- **不使用 --delete**: 02 存在节点级回滚文件 (rollback_tp4-rank1.json / rollback_vllm-envE-worker.json / tp2-worker.json / grafana-dash-bak 等), 严格镜像会误删。脚本为单向增量备份拷贝, 保留 02 本地特有文件。
- 排除 scripts/v027-test (测试残留, **2026-08-17 已删除**: 01 释放 8.2G, 02 释放 6.4G; 删除前 grep 确认仅 mirror 脚本自身引用, 无生产依赖)。
- `--no-o --no-g`: 以 <svc-user> 身份传输, 避免 chown 告警。
- 运行日志: /opt/aicad-prod/scripts/maintenance/mirror.log
- 重跑: 在 01 执行 `bash /opt/aicad-prod/scripts/mirror_to_02.sh` (增量, 秒级)。

## 4. 权限修正记录
- 02 backup 原 root 属主子目录 (scripts-archive/tmp-residue/ring-fix) 已 chown 为 <svc-user>。
- 01 backup/ring-fix-20260811 (600 root) 与 scripts 的 .bak (750 root) 已 chown 为 <svc-user> 以支持非 root rsync 读取。
- 01/02 scripts/maintenance 已 chown 为 <svc-user>。

## 5. 后续建议
- 每周随维护任务可追加 mirror (crontab 可选, 未自动启用 — 因含 39G 大同步, 建议手动按需执行或改为月度)。
- /opt/2hop-s1 原目录已删除 (2026-08-17, tar 校验 diff=0 后), tar 归档已入 backup 双份; 需要恢复时可从 tar 解压。
