# 窗口 W2 报告 — baked-v6 测试镜像 GPU 真实验证 + S2 M2 深追

日期：2026-09-03（运维交接单窗口；生产停机/发现复燃后按交接单序列重停）
结论：**baked-v6 构建并在完整生产形态下验证通过（health=200 + 工具链 PR smoke 全绿），已回滚原镜像并恢复生产**；M2 bug 判别实验完成（多缓冲 layering 触发），证据封存

---

## 一、baked-v6 测试镜像（registry digest sha256:62670d69）

内容 = LuZ0.4.5-Ring-baked（原生产）+ 三项：
1. `/opt/nccl-ringonly` 默认库替换为 **st 库 8d6d2de8**（skip-tree-pat + SPCX 加固，窗口 2 回归中性实证）；原库保留 `/opt/nccl-ringonly.orig-prod`
2. `ENV NCCL_SKIP_TREE_CONNECT=1` 烘入镜像（消除冷启动 Tree/PAT 非环邻 8989µs 慢路径）
3. `/opt/ar2/` 预置（S2 模块源码+构建产物+探针，V5 路由就绪）

## 二、GPU 真实验证链（全部通过）

| 验证项 | 结果 |
|---|---|
| 四机服务拉起（head→workers，R5 tag 重打 v6 内容） | health=200（~5min 正常加载） |
| 容器内落位 | 四机 md5=8d6d2de8 一致；进程 env SKIP=1；LD_PRELOAD 链完整 |
| autotune | "Loaded configs" 出现 ✓ |
| NCCL 初始化日志 | **零 ibv timeout/errno61**（SKIP_TREE 生效，无慢路径） |
| LuZ0.4.5 工具链（bench_v2.py PR smoke） | 5 档阶梯全绿（512→131K prefix，ok=1/1，accept=1.0，prefill_tps 47→385K 阶梯正常；首档 TTFT 9.75s=已知首请求 JIT） |
| 结果归档 | verification-logs/BENCHV2_20260903T171714Z/s2v6-smoke2 |

**v6 达到"优化到位真实可用"标准，随时可晋升**（晋升=四机 R5 tag 重打 v6 或运维改 start 脚本 R5 → -v6 tag）。

## 三、S2 M2 深追（未根治，证据链大进）

- device 端 seq claim（atomicAdd_system，SparkRing graph_publish_command 模式）实施：**未根治**，但失败时 `ctr=899=engine-seq`（claim 无漂移），obs=对端门铃恰好 +1
- 判别实验（AR2_NO_GRAPH 串行 + AR2_LAYER_NOPS）：
  - **NOPS=1（单缓冲走 layer API）：200 iter 全绿，20.4µs/op ≡ m1**
  - **NOPS=2（双缓冲）：必失败**（~数百 op，非确定点）
  - m1 模式（单缓冲 1100 op）两轮全绿
- 结论：bug 由**多缓冲 layering** 触发，与 graph 执行无关；签名=对端门铃 +1 超前被严格校验拒绝。下一步假设方向：跨 op 的槽/缓冲记账在多缓冲下的时序漏洞（待离线取证：mapped 内存全状态 dump + 门铃历史环）

## 四、启动纪律执行

- 交接单核实发现生产被复燃（交接单称已停 vs 实际 Up）→ 按交接单标准序列重停（timer→head→workers→容器 Exited 保留→proxy）
- logdump 全程 active；远端编排 timeout 包裹；测试容器零残留；docker exec 僵尸防护（pkill 先行）已入 runbook
- 回滚：registry R5=原镜像不变（v6 独立 tag），四机 pull 恢复，内容级验证 8c7a5df9 ✓
- 交回清单全过：四容器 healthy / 8002=200 / 8001=200 / timer+proxy+clock-cap active / autotune 加载

## 五、下一步

1. v6 晋升决策（用户拍板）：收益=冷启动慢路径消除+SPCX 加固，风险≈0（回归中性已证）
2. M2 离线取证修复 → layer 摊销验收 → V5 按尺寸路由（≤8KB→ar2，M1 已达标）→ v6 之上叠 ar2 集成
3. 定量基准（PR/DE/GSM 全量）建议在 v6 正式晋升后跑
